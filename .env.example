# Door Math — Distribution Penetration Tracker
# Copy to .env and fill in values

# App
DEBUG=true
PORT=8050

# Database (optional — uses synthetic data by default)
# DATABASE_URL=postgresql://user:pass@localhost:5432/doormath

# Sentry (production only)
# SENTRY_DSN=
